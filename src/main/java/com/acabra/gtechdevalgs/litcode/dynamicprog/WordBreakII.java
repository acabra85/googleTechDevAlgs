package com.acabra.gtechdevalgs.litcode.dynamicprog;

import java.util.*;

public class WordBreakII {

    /**
     *
     * @param s a string to break using spaces to form valid words
     * @param wordDict the dictionary of words
     * @return list of all valid sentences possible from the given string
     */
    public List<String> wordBreak(String s, List<String> wordDict) {
        List<String> sentences = new ArrayList<>();
        StringBuilder sb = new StringBuilder();

        return sentences;
    }
}
